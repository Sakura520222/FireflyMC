# 状态机与并发控制

## 场景
- 区域与模式状态流转复杂（创建/停用/启用/删除）。
- 避免并发冲突（多人同时操作同一区域）。

## 设计方案
- 为 Region/Mode 定义状态机；明确允许的变迁。
- 变迁前检查前置条件，通过事件广播状态变化。
- 并发控制：乐观锁（版本号）或限定在主线程操作。

## 实施步骤
1) 定义状态枚举与变迁表。
2) Manager 负责状态流转校验与持久化。
3) 监听器监听状态事件以更新 UI/缓存。

## 扩展
- 可视化状态与变迁（文档/调试）。
- 状态迁移日志用于审计。