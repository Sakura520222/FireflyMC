# 异步命令与回滚

## 设计要点
- 命令层触发事件，异步执行；主线程仅作薄包装。
- 回滚通过事件溯源：记录可逆事件序列，失败时按序撤销。
- 状态快照辅助回滚；在关键操作前后记录必要快照。

## 实施步骤
1) 定义可逆事件接口（execute/undo）。
2) Manager 记录事件与快照；回滚时反向执行。
3) 失败通知与日志；必要时进入“半回滚”状态并提示管理员。

## 风险与缓解
- 部分回滚时记录不一致状态，提供修复工具。
- 超时/重试策略防止长时间阻塞。