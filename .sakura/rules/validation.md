# 校验与不变量规则

## 输入校验
- 命令层立即校验权限与参数（非空、范围）。
- 配置加载时校验结构与类型，提供默认值与警告。

## 状态不变量
- Region/Mode 存在性检查。
- `board.toKey()` 一致性；跨配置复用同一 key。
- 更新时同步清理关联数据，避免孤立引用。

## 约束执行
- 先校验再执行；校验失败返回错误，不执行副作用。
- 对外接口提供清晰的校验规则文档。