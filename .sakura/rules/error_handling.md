# 错误处理规则

## 通用原则
- 不向服务器传播异常；事件监听器内部捕获并记录。
- 命令通过返回值/回调反馈失败，返回友好消息。
- 禁止空 catch 块，至少记录错误日志。

## 外部依赖失败
- 降级策略：记录错误，返回安全默认值或回滚。
- 配置加载失败：提供默认配置，记录警告。

## 校验
- 命令入口立即校验权限与参数。
- Manager 执行前检查状态不变量（如 Region/Mode 存在性）。

## 日志与诊断
- 错误日志包含上下文（玩家、区域、操作、参数）。
- debug 模式输出关键状态与事件。