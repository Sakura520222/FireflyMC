# 插件开发规则

## 结构与职责
- Plugin 主类负责注册与生命周期；Listener/Command 仅与 Manager 交互。
- GUI 仅作为视图，无状态。

## 禁止事项
- 命令/监听器直接调用其他模块的监听器或命令。
- 在主线程执行阻塞 IO。
- 跨线程共享可变状态。

## 配置与复用
- 通过统一 key（如 `board.toKey()`）复用配置。
- 配置变更触发事件通知，而非直接回调。

## 质量保障
- 单元测试覆盖 Manager 核心逻辑；集成测试模拟事件/命令。
- 代码检查确保异常处理与日志符合规则。