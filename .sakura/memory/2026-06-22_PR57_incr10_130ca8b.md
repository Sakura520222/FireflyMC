# 增量审查深度反思 — PR #57 提交 130ca8b

## 1. 审查质量评估

**覆盖度：局部充分，全局缺失**
- 对 diff 本身的验证完整：逻辑等价性、错误处理路径、后续引用点均已覆盖
- `toLowerCase()` 缺 Locale 的发现是亮点，属于容易被忽略的 i18n 陷阱
- **遗漏**：未扫描同文件是否存在其他 `toLowerCase()`/`toUpperCase()` 调用，属于"同类缺陷批量修复"规则的执行不到位

**准确度：评分偏高**
- 9/10 对一个 4 行变量重命名过于慷慨。重命名方式（`filterId → parsedFilterId → filterId`）本身值得商榷——中间变量仅存活 3 行即赋回原名，增加了理解成本而非降低，这应作为 suggestion 提出
- locale 问题标记为 suggestion 合理，但不影响评分扣分的话，9 分意味着"近乎完美"，与变更实质不符

**完整性：意图理解正确但未质疑价值**
- 正确识别这是纯重构无行为变化
- **缺失**：未质疑这个重构的实际收益。提交消息说"优化解析逻辑"，但实际只是变量换名，与描述存在偏差

## 2. 发现的模式

**代码模式**：
- `tryParse` + null 校验 + 早返回错误码的模式在 AI 工具类中一致使用
- 工具类遵循"解析→校验→执行→返回结果"的标准流水线

**架构观察**：
- AI 模块（function 包）与网络模块（relay/p2p 包）问题密度形成鲜明对比——前者代码质量明显更高，后者是技术债重灾区

## 3. 规范完善建议

**新增规则：字符串大小写转换必须指定 Locale.ROOT**

```
| toLowerCase/toUpperCase 必须指定 Locale | 所有用于比较/匹配/键生成的字符串大小写转换必须使用 Locale.ROOT，禁止无参调用 |
```

理由：本次发现的 `toLowerCase()` 问题具有普遍性，且在 Minecraft mod 环境下（多语言、多 locale 服务器）风险更高。应纳入规则速查表。

**补充规则：重构类提交描述须与实际变更匹配**
提交消息声称"优化解析逻辑"，实际仅为变量重命名。这属于描述失真，虽不阻塞合并但应在评论中指出。

## 4. 经验教训

- **微重构不等于免审**：即使 4 行变更，也要看上下文是否存在同类问题（如其他 toLowerCase 调用）
- **重命名质量需评估**：不是所有重命名都是改进。`A→B→A` 模式的重命名如果中间变量生命周期极短，反而增加噪音
- **提交描述与实质的偏差**是代码卫生的信号，值得在评论中提及

## 5. 增量审查反思

**新增提交是否解决了之前的问题？**
- 历史上下文提到此文件存在"类型过滤参数解析逻辑"问题，本次提交标题声称优化该逻辑
- 但实际只是变量重命名，**并未修复任何逻辑缺陷**。这是一个描述与实质不符的案例，审查应明确指出

**增量变更是否引入新风险？**
- 否，纯重命名无新风险

**覆盖度下降问题——核心反思**
- 本次 PR 总量 74 文件 / +2819 行，增量审查仅看 4 行 diff，覆盖率为 **0.14%**
- 历史遗留的 4 个 major/severe 问题（ModNetwork 空 catch、ClientPayloadHandler 缺校验、ClientState 缺 volatile、AgenticToolLoop 截断错误）在本次审查中再次"不在范围内"
- **这暴露了增量审查在大型 PR 上的系统性失效**：当 PR 规模远超增量变更时，增量审查变成"在冰山上捡雪花"，永远无法触及结构性风险

**改进建议**：
- 当 PR 总变更超过阈值（如 500 行）时，增量审查的 approve 应附带条件：**"本次增量无问题，但 PR 整体仍存在 N 个未解决 major 问题，建议拆分 PR 或在合入前解决"**
- 项目记忆中已标记 `ModNetwork.java` 为"三轮修复失败，需资深开发者介入"，这类问题应在每次相关 PR 的审查结论中强制提醒，而非仅放在"历史遗留问题说明"里被动等待