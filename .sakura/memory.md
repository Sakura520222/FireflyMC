# 项目记忆

## 仓库信息
- 仓库名: Sakura520222/FireflyMC
- 累计反思次数: 6

## 常见代码问题与审查要点

### 日志脱敏完整性
同一方法内所有日志级别（debug/info/warn/error）的输出必须使用相同的脱敏函数。新增日志时需全局检查同文件的其他日志点，避免分级遗漏。

### 脱敏函数性能审查
高频路径（如心跳 1-30s/次）中调用正则或 JSON 重解析的脱敏函数，需评估性能影响，必要时增加缓存或预编译模式。

### 单线程 executor 禁止内部 join
`SingleThreadExecutor` 中调用 `Future.join()` 会导致线程阻塞退化为同步，应改为异步回调或 `get(timeout)`。

### WebSocket/HTTP 回调线程安全
回调默认来自未知线程，`StringBuilder`/`ByteBuffer` 等累加器必须同步，避免竞态条件。

### P2P 握手超时兜底
所有 `CompletableFuture.join()` 必须配套 `orTimeout()` 或 `get(timeout, unit)`，防止打洞卡死。

### 断线重连强制要求
任何网络长连接（WebSocket、TCP）必须有断线重连机制，缺失为 major 级缺陷。

### 静态可变状态生命周期
多个静态字段跨线程访问时，需提供 reset 方法或使用 `volatile`，明确状态重置时机。

## 近期审查模式总结

- **增量审查盲区**：`quick` 模式仅聚焦增量修改，易忽略同模块历史遗留问题（如线程安全、上帝类）。审查报告需明确声明边界。
- **修复类 PR 的隧道视野**：审查者易只验证当前修复，未扫描该模块其他同类风险点。建议强制模板包含“是否波及同模块其他已知问题”。
- **debug 日志隐形风险**：生产环境意外开启 debug 可能泄露敏感信息，所有 debug 日志必须经过脱敏。
- **正则脱敏过度匹配**：通配匹配字段需在 PR 中明确误杀场景及决策原因。

## 规范建议

| 规则 | 说明 |
|------|------|
| 日志脱敏全级别覆盖 | 同一数据源的所有日志级别使用相同脱敏函数 |
| 脱敏函数统一入口 | 封装 `logSafe()` 工具方法，强制所有模块使用 |
| 单线程 executor 禁止 join | 审查发现立即标记 major |
| 静态状态必须可重置 | 多个静态字段需明确生命周期管理 |
| P2P 超时强制兜底 | 所有异步等待必须设置超时 |
| 长连接强制重连 | 缺失断线重连为 major 缺陷 |

## 经验教训

- **增量审查局限性**：多轮小修复可能掩盖底层设计问题，需定期全量健康度评估。
- **脱敏正则范围审查**：宽匹配可能误杀非敏感信息，需权衡安全与可用性。
- **高频路径字符串操作**：虽然单次 GC 压力小，但心跳等高频场景仍需评估累积开销。
- **线程模型默认悲观**：假设所有回调来自未知线程，主动加锁或使用线程安全容器。

## 需要特别关注的领域

- `RelayLobbyWebSocketClient` 线程安全与心跳重连
- `P2PConnectionManager` 竞态条件与超时控制
- 所有 `SingleThreadExecutor` 中的 `join()` 调用
- WebSocket/HTTP 回调中的共享累加器同步
- 配置文件热更新时的任务重新调度
- Bukkit 主线程消息发送的 `server.execute()` 委托模式