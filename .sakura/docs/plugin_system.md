# 插件系统

## 生命周期
- `onEnable()`: 初始化 Manager、加载配置、注册监听器与命令。
- `onDisable()`: 清理缓存、任务、注销监听器/命令，持久化必要状态。

## 模块初始化顺序
- 依赖方在被依赖方初始化后才加载；通过注入/事件总线解耦。
- 配置加载→不变量校验→监听器注册。

## 扩展点
- 事件机制用于功能扩展（RegionEvent、ModeEvent）。
- 新模式/区域类型通过注册机制加入，不修改现有模块。

## 调试与诊断
- 提供 debug 模式输出关键事件与状态变更。
- 结构化日志包含上下文（玩家/区域/操作）。