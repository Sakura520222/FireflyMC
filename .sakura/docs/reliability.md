# 可靠性与容错

## 异常处理
- 事件层捕获异常，不传播到服务器。
- 命令层通过返回值/回调反馈失败；避免静默吞掉错误。
- 外部依赖失败时降级：记录错误日志，返回安全默认值。

## 线程安全
- 同步事件使用主线程数据结构；跨线程通过 scheduleSyncDelayedTask 等回调。
- 不可变数据结构用于缓存与配置读取。

## 资源清理
- 监听器/命令注销时清理缓存/任务。
- 异步任务保证取消与状态重置，防止泄漏。

## 校验与防御
- 输入参数非空检查与类型匹配。
- 依赖状态（Region/Mode 存在性）先检查再执行。