# 持久化与事件溯源

## 存储方式
- Bukkit 配置 API 用于简单配置。
- 用于结构化数据与热重载；支持增量变更与历史回溯。

## 事件与状态
- 状态变更通过内部事件广播（如 RegionCreated、RegionRemoved），而非命令/监听器直接耦合。
- Command 层进行权限与参数校验，并产生事件；Manager 监听事件并协调持久化。

## 不变量
- `board.toKey()` 一致性：无论配置来源，key 必须相同。
- 持久化前校验（不存在则创建、存在则更新）与原子性（单个 Region/Mode 操作原子）。