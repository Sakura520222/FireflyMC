# 配置与数据模型

## 配置职责分离
- `board.yml`: 玩家视图（展示与信息），非规则定义。
- `region.yml`: 区域规则，聚合 `board` 的展示 key。

## 复用与校验
- `board.toKey()` 提供统一 key，多配置复用；加载后即校验 key 一致性。

## 数据一致性
- 更新 Region 规则时同步清理关联的 board 展示项。
- 配置重载应保证不变量：region 必存在时才关联 board 展示。

## 可维护性
- 避免魔法字符串，用枚举/常量定义配置键。
- 提供 schema/默认配置示例，以防止误配置。